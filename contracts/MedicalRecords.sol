//pragma solidity ^0.5.0;

contract MedicalRecords{

    // medical record structure

    // medical record events

    // medical record modifiers

    // medical record functions

    // function to create medical record
    function createRecord(address patient, int cost) public {}

    // function to view medical record
    function viewRecord(int recordId) public {}

    // function to grant user access for this record
    function grantAccess(address user) public {}

    // function to remove user access for this record
    function ungrantAccess(address user) public {}
}